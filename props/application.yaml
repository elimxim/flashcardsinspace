server:
  port: ${APP_PORT}
  ssl:
    key-store-type: PKCS12
    key-store: file:${APP_SSL_KEYSTORE_PATH}
    key-store-password: ${APP_SSL_KEYSTORE_PASSWORD}
    key-alias: ${APP_SSL_KEYSTORE_ALIAS}

spring:
  datasource:
    url: ${APP_DATASOURCE_URL}
    username: ${APP_DATASOURCE_USERNAME}
    password: ${APP_DATASOURCE_PASSWORD}

app:
  security:
    jwt:
      secret: ${APP_SECURITY_JWT_SECRET}
      access-token-expiration-ms: 7200000 # 2 hours
      refresh-token-expiration-ms: 86400000 # 24 hours